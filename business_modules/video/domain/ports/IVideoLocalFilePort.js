/**
 * Port: resolve a repo-local media file path (manual copy-paste into input/files) safely.
 *
 * Implementations: localVideoFileAdapter.
 */

export const VIDEO_LOCAL_FILE_PORT = Symbol('IVideoLocalFilePort');
