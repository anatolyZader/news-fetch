/**
 * Port: fetch remote video/audio and extract to a local file (e.g. yt-dlp → MP3).
 *
 * Implementations: ytDlpYoutubeAdapter.
 */

export const VIDEO_REMOTE_FETCH_PORT = Symbol('IVideoRemoteFetchPort');
