# Doc-drift audit — 2026-07-29

Scanned: 65 files (docs/ 37 md, .claude/commands/ 25, AGENTS.md, CLAUDE.md, memory.md) | Citations checked: 798 | Dead (high-confidence, root-anchored): 96 | Judged from unresolved-relative: 3 promoted, rest are historical shorthand or non-path noise

> Report-only artifact produced by `/audit-docs`. No files were modified. Fixes below are proposals for operator approval.

## Executive Summary

Drift is concentrated in one event: the `resilience_scorer` restructuring (contracts moved from `cross-cut-modules/resilience-contracts/` into `business_modules/resilience_scorer/domain/contracts/`, and `domain/services/` split into `signals/`, `operator/`, `oov/` etc.). That single move accounts for the majority of the 96 dead citations, spread across 13 docs and one slash command. The most urgent fix is operational, not cosmetic: **`/review-signals` is broken today** — its embedded script `require()`s two deleted contract paths, so the command fails at Step 2. Second-order drift: a wrongly-cited reports directory (`data/reports/` vs actual `data/daily_reports/`), stale version strings (catalog `v6` in docs vs `v8` in code; `extract-v2` vs `extract-v3`), and an ADR numbering collision. The `updated_main_docs/` generation and the Figma trio carry heavy drift but are already flagged superseded/stale in `docs/INDEX.md` — fixing them is lower value than deciding their fate.

## Dead-path citations (grouped by root cause)

### 1. `cross-cut-modules/resilience-contracts/` → moved to `business_modules/resilience_scorer/domain/contracts/`

| Cited path | Where | Verdict / proposed fix |
|---|---|---|
| `…/signalCatalog.js`, `…/resilienceComponents.js` | **`.claude/commands/review-signals.md`** (inside `require()` — command broken) | moved (`25536895`) → `./business_modules/resilience_scorer/domain/contracts/signalCatalog.js` etc. |
| `…/extractionPrompt.js` | `docs/MODEL-CARD.md`, `docs/main_docu_files/COST-CONTROLS.md`, ADR `002-signal-scope-attribution` | moved → `domain/contracts/extractionPrompt.js` |
| `…/signalCatalog.js` | `RESILIENCE-ENGINE-REFERENCE.md`, `reviews/application-architecture-and-analysis-deep-dive.md` | moved → `domain/contracts/signalCatalog.js` |
| `…/reportSelection.js` | `SYSTEM-AND-OPERATOR-MODEL.md` | moved → `domain/contracts/reportSelection.js` (see Contradictions §) |
| `…/displayViews.js`, `…/assessmentV2.js`, bare `cross-cut-modules/resilience-contracts/` | `updated_main_docs/` 00, 01, 02, 04, 05, 07 | moved → `domain/contracts/…` (or defer — whole dir is superseded) |

### 2. `domain/services/` flattening → subfolders (same doc set)

| Cited path (old) | Actual location now |
|---|---|
| `domain/services/regionSignalFilter.js` | `domain/services/signals/regionSignalFilter.js` |
| `domain/services/signalDistrictId.js` | `domain/services/signals/signalDistrictId.js` |
| `domain/services/signalRouter.js` | `domain/services/signals/routing/signalRouter.js` |
| `domain/services/assessmentDisplayTier.js` | `domain/services/operator/assessmentDisplayTier.js` |
| `domain/services/decisionBriefPrompt.js` | `domain/services/operator/decisionBriefPrompt.js` |
| `domain/services/attentionItems.js`, `…/componentDiagnostics.js` | `domain/services/operator/…` |
| `domain/services/oovCapture.js` | `domain/services/oov/oovCapture.js` |
| `domain/services/actionCompass.js` | `domain/services/actionCompass/actionCompass.js` |
| `domain/services/thinEvidencePolicy.js` | `domain/epistemic/thinEvidencePolicy.js` |

Affected docs: `GEOGRAPHIC-ANALYSIS.md`, `RESILIENCE-ENGINE-REFERENCE.md`, `SYSTEM-AND-OPERATOR-MODEL.md`, `reviews/geographic-analysis-implementation.md`, `reviews/application-architecture-and-analysis-deep-dive.md`, `updated_main_docs/` 01–05, 07.

### 3. `app/` reorg

| Cited path (old) | Actual location now | Where |
|---|---|---|
| `app/pipelineOrchestrator.js`, `app/pipelineIngestPlan.js` | `app/pipeline/…` | `updated_main_docs/02` |
| `app/loadOpenObservationsForAssess.js` | `app/assessment/loadOpenObservationsForAssess.js` | `updated_main_docs/02` |
| `app/reportCacheService.js` | `infrastructure/reportCacheService.js` | `LLM-CHAT-AND-AGENTS.md`, `SYSTEM-AND-OPERATOR-MODEL.md`, `updated_main_docs/` 01, 06 |
| `cross-cut-modules/llm/cache/extractionCacheIntegration.js` | `business_modules/resilience_scorer/infrastructure/extractionCacheIntegration.js` | `COST-CONTROLS.md` |
| `cross-cut-modules/evidence/input/evidenceRoutes.js` | `business_modules/evidence_submission/input/evidenceRoutes.js` | `COST-CONTROLS.md` |

### 4. PBO module rename: `pbo_report_muni/` + `pbo_report_regional/` → `pbo_report/`

`PIPELINE-AND-SOURCES.md` (3 citations), `updated_main_docs/03` (3 citations). All scripts exist under `business_modules/pbo_report/input/`.

### 5. Reports directory misnamed: `data/reports/` → `data/daily_reports/`

Cited in `.claude/commands/review-signals.md` (path resolution step + example filename), `updated_main_docs/` 06, 07, `reviews/8-component-…-notebooklm.md`, `reviews/application-architecture-…`. Also `memory.md` cites `resilience/data/reports/` (shorthand — verify intent before touching).

### 6. Deleted outright (reword or drop the citation — nothing to point at)

- **Shadow-scoring residue**: `produceAssessmentWithShadow.js`, `shadowArtifactsFileAdapter.js`, `scoringPipelinePrep.js`, `anomalyStrip.js` — cited in `RESILIENCE-ENGINE-REFERENCE.md`, `updated_main_docs/04`, `PIPELINE-AND-SOURCES.md` (relative). Docs describe shadow/divergence artifacts as still written.
- **Removed epistemic modules**: `peaceTimeAnchors.js` (MODEL-CARD), `massContribution.js`, `componentItems.js`, `evidenceCaps.js`, `certaintyTuning.js` (`updated_main_docs/05`), `pipelineArtifactPaths.js` (`updated_main_docs/` 02, 03, 07), `scoringFacade.js` (`updated_main_docs/` 01, 05, 07), `assessmentPipeline.js` (`GEOGRAPHIC-ANALYSIS.md`), `componentFacets.js` (`main_docu_files/README.md` docs:sync source list, `RESILIENCE-ENGINE-REFERENCE.md`, notebooklm review), `backfillReportBriefMdCli.js` (`updated_main_docs/06`), `behaviorSignals.js` (ADR 002-llm-extract, deep-dive review).
- **Deleted client files**: `AutoLayout.jsx` (both Figma docs), `DocsPanel.module.css`, `ReportBuildPanel.module.css`, `SubmissionsTab.jsx` (`ui-componentization-audit.md`).
- **Deleted misc**: `.cursor/skills/create-business-module/SKILL.md` (**`AGENTS.md`** + `specs/resilience-business-module.md`), `business_modules/resilience_scorer/analyst/README.md` (**`AGENTS.md`** — analyst UI deleted), `analyst/` dir refs in `updated_main_docs/` 01, 05, 07, `data/connectivity-probes/` (MODEL-CARD), `tests/business_modules/resilience` (deep-dive review), `cross-cut-modules/geo/input/runAnalyzeSurvey.js` (geo review; live copy now only in `archive/survey-excel-cli/`).

### 7. Broken relative links

- `architecture/decisions/004-deployment-topology.md` → `../../cross-cut-modules/docs/content/pages/operations/deployment-split.md`: target exists; depth is one `../` short (needs `../../../`).
- Historical `../../…` refs in `reviews/` deep-dive and notebooklm docs point at moved files (same root causes as §1–2).

Self-check: the two `docs/INDEX.md` citations flagged during extraction (from the new `compile-docs`/`audit-docs` commands) resolved once the index was generated — correctly excluded here.

## Index freshness

`docs/INDEX.md` generated today (2026-07-29) — all 42 files present; no ghost entries. Nothing to fix.

## Contradictions & generation drift

- **Version strings**: code has `CATALOG_VERSION = 'v8'` (`domain/contracts/signalCatalog.js`) but `architecture/ubiquitous-language.md` and hand-maintained body tables of `RESILIENCE-ENGINE-REFERENCE.md` say **v6**; code has `EXTRACT_PROMPT_VERSION = 'extract-v3'` but `MODEL-CARD.md` and `COST-CONTROLS.md` say **extract-v2** (ADR 002-signal-scope-attribution correctly says v3).
- **`SYSTEM-AND-OPERATOR-MODEL.md`** claims `reportSelection.js` is "not yet imported by production code" — the file now lives in `domain/contracts/`; the claim needs re-verification, not just a path fix.
- **docs:sync overpromise**: `docs/README.md` implies component tables are auto-synced, but sync markers only cover the appendix of `RESILIENCE-ENGINE-REFERENCE.md`; the body tables (incl. the v6 claim) are hand-maintained and have drifted.
- **ADR numbering**: two ADRs numbered 002 (`002-llm-extract-code-score`, `002-signal-scope-attribution`); ADR 001 forward-references an "ADR 004" about report-scope policy, but 004 is deployment topology.
- **Legacy labeling is inconsistent**: `updated_main_docs/00` declares itself the successor of the notebooklm review; `main_docu_files/README.md` also labels that review legacy yet still lists it in the NotebookLM upload bundle; neither generation mentions the other.

## Recommended fixes (ranked by impact)

1. **Repair `/review-signals`** — in `.claude/commands/review-signals.md`, change the two `require('./cross-cut-modules/resilience-contracts/…')` to `./business_modules/resilience_scorer/domain/contracts/…`, and the reports dir/example to `data/daily_reports/`. Impact: the command errors at Step 2 today.
2. **Sweep `cross-cut-modules/resilience-contracts/` → `domain/contracts/`** in the live docs (`MODEL-CARD.md`, `COST-CONTROLS.md`, `RESILIENCE-ENGINE-REFERENCE.md`, `SYSTEM-AND-OPERATOR-MODEL.md`, ADR 002-signal-scope-attribution). Impact: the most-cited dead path; misleads every code lookup.
3. **Fix `data/reports/` → `data/daily_reports/`** wherever cited (see §5). Impact: operators/agents looking for report artifacts in a nonexistent directory.
4. **Update version strings** v6 → v8 and extract-v2 → extract-v3 (4 files, §Contradictions). Impact: epoch/version reasoning depends on these; your scoring-epoch memory notes make stale versions actively harmful.
5. **Fix `main_docu_files/` service paths** per §2–3 tables (GEOGRAPHIC-ANALYSIS, RESILIENCE-ENGINE-REFERENCE, SYSTEM-AND-OPERATOR-MODEL, LLM-CHAT-AND-AGENTS, PIPELINE-AND-SOURCES incl. `pbo_report` rename). Impact: live reference generation should be trustworthy file-by-file.
6. **Fix `AGENTS.md` dead refs** (`.cursor` skill, `analyst/README.md`). Impact: the constitution file every session reads first.
7. **ADR hygiene** — renumber one of the 002s (suggest `002-llm-extract-code-score` → `005`, it's the thinner note), fix ADR 001's forward reference, fix ADR 004's link depth. Impact: low, but cheap.
8. **Decide fate of stalled/stale sets** rather than patching them: `updated_main_docs/` (8 files, heaviest drift density), Figma trio + `ui-componentization-audit.md` (cite deleted client files), `specs/ynet-articles-api.md`. Options: archive/, add a legacy banner, or delete. Patching their paths is low-value until this decision is made.
9. **Re-verify prose claims** flagged in Contradictions (reportSelection import status; shadow/divergence artifacts described as still written; RAG catalog-reindex CLI wiring). These need code checks, not find-and-replace.
