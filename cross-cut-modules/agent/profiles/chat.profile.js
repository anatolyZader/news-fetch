/**
 * Chat agent profile — tools supplied at runtime by createChatToolContext.
 */
import { registerProfileTools } from '../toolRegistry.js';

registerProfileTools('chat', []);

export const CHAT_PROFILE_ID = 'chat';
